
/*
  trait App {
    def mainIO: IO[Unit]
    def rts: RTS = defaultRTS

    // TODO Feed arguments with the STM in IO? Or find
    def main(args: Array[String]): Unit = rts.unsafePerformIO_(mainIO)
  }

  */
